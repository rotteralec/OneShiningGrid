import React from "react";
import ReactDOM from "react-dom/client";
import OneShiningGrid from "../OneShiningGrid.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <OneShiningGrid />
  </React.StrictMode>
);
